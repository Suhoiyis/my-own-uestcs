
# 8.2.1 低通模拟信号的抽样定理


### 1. 什么是抽样 (Sampling)？

抽样就是在某些特定的时刻抽取模拟信号的值，将其从“时间连续”的信号变成“时间离散”的样值序列

### 2. 抽样定理（奈奎斯特第一准则）—— 必背考点

这是本章最重要的结论。

- 定理内容：对于一个最高频率为 $f_H$（或带宽 $B$）的带限模拟信号 $m(t)$，如果抽样频率 $f_s$ 满足：
    
    $$f_s \ge 2f_H$$
    
    那么，这个样值序列就可以**无失真地**还原出原始模拟信号
    
- 奈奎斯特速率 (Nyquist Rate)：
    
    $$f_N = 2f_H$$
    
    这是理论上允许的**最低**抽样频率
    

---

### 3. 为什么是 2 倍？（频域分析）

课件第 7-8 页用频谱图完美解释了这个“2倍”的来源。

- 时域操作：抽样相当于用模拟信号 $m(t)$ 去乘以一个周期性冲激脉冲序列 $\delta_T(t)$ 4。
    
    $$m_s(t) = m(t) \cdot \sum \delta(t - nT_s)$$
    
- 频域结果（搬移与复制）：
    
    时域相乘 = 频域卷积。
    
    冲激序列的频谱也是冲激序列（间隔为 $f_s$）。卷积的结果就是把原信号的频谱 $M(f)$ 在频率轴上每隔 $f_s$ 复制一份 5。
    
    $$M_s(f) = \frac{1}{T_s} \sum_{n} M(f - n f_s)$$
    

**关键图解（P8）**：

- 原始频谱宽度是 $2f_H$（从 $-f_H$ 到 $+f_H$）。
    
- 复制后的频谱中心分别在 $0, \pm f_s, \pm 2f_s \dots$。
    
- **如果不混叠**：这就要求相邻频谱的边缘不能“打架”。
    
    - 前一个谱的右边缘是 $f_H$。
        
    - 后一个谱（中心在 $f_s$）的左边缘是 $f_s - f_H$。
        
    - 为了不重叠，必须满足：$f_s - f_H \ge f_H \Rightarrow f_s \ge 2f_H$ 6。
        

> **混叠现象 (Aliasing)**：如果 $f_s < 2f_H$，频谱就会发生重叠，高频成分会混入低频，导致信号无法恢复 7。

---

### 4. 怎么还原？（重建过程）

既然抽样后的频谱是原频谱的周期延拓，我们要还原信号，只需要把**中间那个原始频谱**（低频部分）切出来就行了。

- **工具**：一个**理想低通滤波器 (LPF)**。
    
    - 截止频率：$f_H$（或 $f_s/2$）。
        
    - 增益：$T_s$ 8。
        
- 时域内插公式：
    
    课件第 10-11 页给出了时域的重建公式。通过 LPF 后，每个样值点 $m(nT_s)$ 都变成了一个 Sa 函数（内插函数）。所有这些 Sa 函数叠加起来，就完美还原了原始波形 $m(t)$ 9。
    
    $$m(t) = \sum_{n} m(nT_s) \frac{\sin[\pi f_s (t - nT_s)]}{\pi f_s (t - nT_s)}$$
    

---

### 📝 8.2.1 总结笔记

- **核心条件**：$f_s \ge 2f_H$。
    
- **物理过程**：时域离散化 $\leftrightarrow$ 频域周期化。
    
- **还原方法**：低通滤波器 (LPF)。
    

这一节是理想情况（冲激抽样）。但在现实中，我们没有无限窄的冲激脉冲，只能用有一定宽度的矩形脉冲来抽样。

这就引出了下一节：实际抽样（自然抽样与平顶抽样）。