

在讲具体的 5.1 之前，我们先看为什么要“调制”：

1. **频谱搬移**：将低频的基带信号搬移到高频，以便天线发射（天线长度需与波长匹配）。
    
2. **频分复用**：让不同的广播电台占用不同的频率，互不干扰。
    
3. **提高抗干扰能力**：通过不同的调制方式（如 FM）来抵抗环境噪声。
    

### 三个基本名词：

- **调制信号 $m(t)$**：我们要传输的原始信息（比如你的声音）。
    
- **载波 $c(t)$**：用来“运载”信息的信号，通常是高频正弦波 $A_c \cos(\omega_c t + \phi)$。
    
- **已调信号 $s(t)$**：调制后的结果，即我们要发往空中的信号。

![[Pasted image 20260103214829.png]]![[Pasted image 20260103214842.png]]





# 5.1 幅度调制 (Amplitude Modulation, AM) 的原理

**幅度调制**（又称线性调制）的本质是：**让载波的幅度随调制信号 $m(t)$ 的变化而变化**。

![[Pasted image 20260103215012.png]]

根据频谱利用率和功率分配的不同，5.1 细分为四种方式：

## 1. 标准调幅 (AM) —— 最经典的广播方式

这是最简单的调制方式，收音机最常用的就是它。

### 1.时域表达式：

$s_{AM}(t) = [A_0 + m(t)] \cos(\omega_c t)$
    
    - 其中 $A_0$ 是外加的直流分量，确保 $[A_0 + m(t)] > 0$。

- **$m(t)$**：调制信号（假设平均值为 0）。
    
- **$A_0$**：外加的直流分量。
    
- **$\cos(\omega_c t)$**：高频载波。



![[Pasted image 20260103215244.png]]
        
#### 2. 调幅指数 (Modulation Index) —— $m_a$

这是一个衡量调制“深浅”的关键指标：

$$m_a = \frac{|m(t)|_{max}}{A_0}$$

- **正常调制 ($m_a \le 1$)**：$A_0 \ge |m(t)|_{max}$。此时已调信号的包络线（外轮廓）与原信号 $m(t)$ 的形状完全一致。
    
- **过调制 ($m_a > 1$)**：$A_0 < |m(t)|_{max}$。此时包络线会发生交叉，波形出现失真，简单的检波器无法还原声音，会出现“破音”。

![[Pasted image 20260103215600.png]]


---

#### 3. 波形特点


- **外形**：看起来像一个上下对称的“梭子”。
    
- **包络**：它的上边界曲线就是 $A_0 + m(t)$。只要没有过调制，我们只需要追踪这个边界就能找回信息。

![[Pasted image 20260103215543.png]]

---


#### 4. 频域分析（频谱）

对时域公式做傅里叶变换，得到 $S_{AM}(f)$：

$$S_{AM}(f) = \frac{1}{2}A_0 [\delta(f-f_c) + \delta(f+f_c)] + \frac{1}{2} [M(f-f_c) + M(f+f_c)]$$

- **载波分量**：在 $f_c$ 处有两个孤立的冲击点 $\delta$，代表纯载波功率。
    
- **上下边带**：原信号频谱 $M(f)$ 被搬移到了 $f_c$ 的左右两侧。
    
- **带宽 $B_{AM}$**：若 $m(t)$ 的最高频率为 $f_m$，则 AM 信号带宽为 **$2f_m$**。
    
![[Pasted image 20260103215819.png]]

---

#### 5. 功率分配

AM 的总功率 $P_{AM}$ 分为两部分：

$$P_{AM} = P_c（载波功率） + P_s（边带功率）$$

- **载波功率 $P_c$**：占大头，但不携带任何信息。
    
- **边带功率 $P_s$**：真正携带信息的功率。
    
- **效率 $\eta$**：$\eta = \frac{P_s}{P_c + P_s}$。
    
    - 即使在 $m_a = 1$（ 满调制）且信号是单音正弦波时，**最大效率也只有 1/3 (33.3%)**。大部分能量都浪费在那个不带信息的载波上了。
        
![[Pasted image 20260103215856.png]]

---

#### 6. 产生与接收（解调）

##### 产生：

通常使用**相乘器**或**非线性器件**实现。将 $[A_0 + m(t)]$ 与载波相乘即可。

##### 接收（解调）—— AM 的核心优势：

- **方法：包络检波 (Envelope Detection)**
    
- **电路**：二极管 + RC 低通滤波器。
    
- **原理**：利用二极管的单向导电性删掉负半周，再利用电容的充放电滤掉高频载波，剩下的“锯齿波”平滑后就是 $m(t)$。
    
- **优点**：接收机极其简单、便宜。

![[Pasted image 20260103215942.png]]

---

#### 📝 AM 总结笔记

- **最大的优点**：接收设备简单（只需二极管检波）。
    
- **最大的缺点**：功率利用率低（大部分能量在载波上），带宽浪费（$2f_m$）。


        

## 2. 双边带调制 (DSB-SC) —— 省去载波的 AM

DSB 的核心思路只有一个：**既然载波 $A_0$ 不携带信息且极其耗电，那我们就直接把它“干掉”。**

---

### 1. DSB 表达式（时域）

DSB 的时域公式就是简单的调制信号与载波相乘：

$$s_{DSB}(t) = m(t) \cos(\omega_c t)$$

- **$m(t)$**：调制信号（基带信号），通常假设其均值为 0。
    
- **$\cos(\omega_c t)$**：高频载波。

![[Pasted image 20260103220134.png]]


### 2. 波形特点与“相位反转”

- **外形**：DSB 的波形包络不再直接对应 $m(t)$ 的形状。
    
- **相位跳变**：当 $m(t)$ 跨越零点（由正变负或由负变正）时，已调信号的载波相位会发生 **180° 的突变**。
    
- **后果**：由于这种相位反转，DSB 信号的包络不再总是正的，因此**无法使用简单的包络检波器**还原信号。
    
![[Pasted image 20260103220202.png]]

### 3. 频域分析（频谱）

对 $s_{DSB}(t)$ 做傅里叶变换：

$$S_{DSB}(f) = \frac{1}{2} [M(f-f_c) + M(f+f_c)]$$

- **抑制载波**：与 AM 不同，DSB 的频谱在 $f_c$ 处**没有**那个高耸的冲击点 $\delta(f-f_c)$。
    
- **带宽 $B_{DSB}$**：依然是基带信号最高频率 $f_m$ 的两倍，即 **$2f_m$**。
    
- **信息分布**：频谱由对称的上边带（USB）和下边带（LSB）组成。

![[Pasted image 20260103220240.png]]

### 4. 功率分配与效率

- **功率公式**：$P_{DSB} = \frac{1}{2}\overline{m^2(t)}$。
    
- **效率**：因为没有发射纯载波，所有的功率都集中在携带信息的边带上，所以其**调制效率为 100%**。
![[Pasted image 20260103220228.png]]


### 5. 产生与接收（解调）

#### 产生方法：相乘器

在硬件上，DSB 通常通过**环形调制器**或乘法器直接将 $m(t)$ 与载波相乘得到。

#### 接收方法：相干解调 (Coherent Demodulation)

这是 DSB 最头疼的地方。由于包络检波失效，必须使用相干解调：

1. **乘法**：将接收到的 $s_{DSB}(t)$ 再次乘以一个**本地载波** $\cos(\omega_c t)$。
    
2. **低通滤波**：通过 LPF 滤掉高频成分，还原出 $m(t)$。
    
3. **核心难点**：本地载波必须与发送端的载波**同频、同相**。如果相位差了 90°，输出信号就会变为 0（这种现象叫正交抵消）。

![[Pasted image 20260103220537.png]]![[Pasted image 20260103220544.png]]

---

### 📝 DSB 与 AM 的深度对比笔记

| **特性**         | **AM (标准调幅)** | **DSB (抑制载波双边带)**  |
| -------------- | ------------- | ------------------ |
| **直流分量 $A_0$** | 有             | 无                  |
| **功率效率**       | 低 (最高 33%)    | **高 (100%)**       |
| **带宽**         | $2f_m$        | $2f_m$             |
| **接收复杂度**      | 极简单 (包络检波)    | 复杂 (相干解调)          |
| **主要应用**       | 中短波广播         | 点对点通信、作为 SSB 的中间步骤 |


---

**DSB 实际上是“拿接收机的复杂度换取发射机的省电”。** 既然 DSB 已经把载波去掉了，大家自然会想：既然左右两个边带是对称的，信息重复了，能不能**再拆掉一半边带**来节省带宽？这就是我们接下来要讲的 **SSB（单边带调制）**


        

### 3. 单边带调制 (SSB) —— 最省带宽的方式

SSB 是模拟幅度调制中**频谱效率最高**的一种方式。

---

#### 1. SSB 的时域表达式

SSB 的表达式相对复杂，因为它涉及到了我们在第二章学过的**希尔伯特变换（Hilbert Transform）**：

$$s_{SSB}(t) = \frac{1}{2}m(t)\cos\omega_ct \mp \frac{1}{2}\hat{m}(t)\sin\omega_ct$$

- **符号含义**：
    
    - **减号 ($-$)**：对应**上边带 (USB)**。
        
    - **加号 ($+$)**：对应**下边带 (LSB)**。
        
    - **$\hat{m}(t)$**：$m(t)$ 的希尔伯特变换（即将 $m(t)$ 每个频率分量相移 $-90^\circ$）。
        
![[Pasted image 20260103221540.png]]
![[Pasted image 20260103221554.png]]
---

#### 2. 频域分析（频谱）

- **带宽 $B_{SSB}$**：这是 SSB 最大的优势。它的带宽仅为基带信号最高频率 $f_m$，即 **$B_{SSB} = f_m$**。
    
    - 对比：AM 和 DSB 的带宽都是 $2f_m$。
        
- **频谱位置**：USB 位于载频 $f_c$ 以上，LSB 位于 $f_c$ 以下。由于去掉了载波和另一个边带，频谱非常“干净”。
    

---

#### 3. 调幅指数与功率分配

- **调幅指数**：在 SSB 中，包络不再直接对应 $m(t)$，因此通常不讨论传统意义上的 AM 调幅指数。
    
- **功率分配**：SSB 抑制了载波和多余的一个边带。其功率效率比 AM 高得多，且比 DSB 节省了一半的发射功率（因为只发一个边带）。
    

---

#### 4. SSB 的产生方法（两种主流技术）

产生 SSB 信号是模拟技术中的难点，主要有以下两种方法：

##### ① 滤波法 (Filter Method)

- **原理**：先产生一个 DSB 信号（抑制载波双边带），然后通过一个极其陡峭的**边带滤波器**，滤掉不想要的那个边带。
    
- **难点**：对滤波器的要求非常高。如果基带信号 $m(t)$ 有很低的直流成分，边带滤波器很难在 $f_c$ 附近切得非常干净。
    

##### ② 相移法 (Phase-shift Method)

- **原理**：直接利用时域表达式。让 $m(t)$ 经过一个 $90^\circ$ 相移器得到 $\hat{m}(t)$，同时让载波也相移 $90^\circ$，然后分别相乘再相加/减。
    
- **难点**：要求相移器在整个信号频带内都能精确地维持 $90^\circ$ 相移，硬件实现较复杂。
    

---

#### 5. 接收与解调

- **方式**：**相干解调 (Coherent Demodulation)**。
    
- **要求**：同 DSB 一样，接收端必须恢复出一个与发送端完全**同步（同频同相）**的本地载波。
    
- **缺点**：如果本地载波频率有偏差，解调出来的声音会变调；如果有相位差，信号强度会减弱。
    ![[Pasted image 20260103221625.png]]

---

#### 📝 SSB 核心总结笔记

|**特性**|**SSB (单边带)**|**评价**|
|---|---|---|
|**带宽**|**$f_m$**|**最省带宽**，频谱利用率最高|
|**功率效率**|极高|能量全部用于有效边带传输|
|**波形包络**|不随 $m(t)$ 变化|不能用包络检波，必须相干解调|
|**主要用途**|短波通信、载波电话、军事电台|适合资源受限、距离远的场景|

---

**思考题：** 既然 SSB 这么省带宽和功率，为什么我们的普通广播（AM）不改用 SSB 呢？

- **答案：** 因为 SSB 接收机需要复杂的同步载波和相干解调电路，太贵了！相比之下，AM 只需要一个二极管检波，普通老百姓都能买得起。
    

### 4. 残留边带调制 (VSB) —— 折中方案

SSB 滤波器很难做得很陡（边缘太硬），所以我们稍微留一点点另一侧的边带，这就叫残留边带。

- **用途**：经典的**地面模拟电视信号传输**。
    

---

# 5.1 幅度调制总结与对比

1. 核心数学模型复习

- **AM**: $s(t) = [A_0 + m(t)] \cos(\omega_c t)$
    
- **DSB**: $s(t) = m(t) \cos(\omega_c t)$
    
- **SSB**: $s(t) = \frac{1}{2}m(t) \cos(\omega_c t) \mp \frac{1}{2}\hat{m}(t) \sin(\omega_c t)$
    

2. 性能横向对比（重点笔记） 

| **特性**     | **AM (标准调幅)**   | **DSB (双边带)** | **SSB (单边带)**  |
| ---------- | --------------- | ------------- | -------------- |
| **带宽 $B$** | **$2f_m$**      | **$2f_m$**    | **$f_m$** (最省) |
| **功率效率**   | 低 (大部分在载波)      | 高 (100%)      | **最高**         |
| **解调方式**   | **包络检波** (最简单)  | 相干解调 (复杂)     | 相干解调 (最复杂)     |
| **抗噪声性能**  | 最差              | 较好            | 与 DSB 相当       |
| **应用场景**   | **广播** (省接收机成本) | 点对点通信         | **短波通信、多路复用**  |

![[Pasted image 20260103221646.png]]




### 💡 老师可能会问的“为什么”：

1. **为什么广播用 AM？**
    
    - 因为广播的特点是“一个发，万个收”。虽然发射机浪费点电，但能让千家万户用上最便宜、最简单的二极管收音机（包络检波） 18。
        
2. **为什么载波电话和军事电台用 SSB？**
    
    - 因为频谱资源和功率极其宝贵。SSB 带宽减半，可以在同样的频率范围内塞进两倍数量的用户
        

---

## 接下来该讲什么？

既然“幅度”已经玩出花来了（线性调制），下一步就要动载波的**角度**了。

按照课件顺序，我们即将进入：

**5.2 角度调制（非线性调制）原理**

这部分包含：

- **PM (相位调制)**：让相位跟着声音跳舞 21。
    
- **FM (频率调制)**：让频率跟着声音快慢变化，这是目前高清广播和对讲机的核心 22。
    
- **WBFM (宽带调频)**：涉及复杂的**贝塞尔函数**（这一章的数学难点） 23。





1.标准幅度调制（AM）
发端:上边带和下边带均被发送，并伴有载波。
收端:对AM信号的解调很简单，如采用包络检波。
应用:广泛应用于商用AM无线广播中，无线广播
系统包括一个功率发射机和很多造价低廉的
接收机。

2.抑制载波的双边带（DSB-SC）调制
发端:只发送上边带和下边带。发送同样的消息信
号时，需要的功率远小于标准AM的发射功率。
收端:要求相干解调,接收机结构相对复杂。
应用:适用于只有一个发射机和一个接收机的点对
点通信。

3.单边带（SSB）调制
发端:只发送上边带或下边带。需要的信道带宽最小。
收端:要求相干解调,接收机结构相对复杂。
应用:只能应用于能量间隙中心在零频率的消息信号。
常用于二次调制。